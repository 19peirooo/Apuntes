apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  type: NodePort
  selector:
    app: server
  ports:
  - protocol: TCP
    port: 32001
    targetPort: 32001
    nodePort: 32001
  externalTrafficPolicy: Cluster
