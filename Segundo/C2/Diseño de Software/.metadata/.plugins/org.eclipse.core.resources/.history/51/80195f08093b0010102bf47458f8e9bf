package objectadapterpattern.socketStrategy;

public class EnergySocket120VStrategy implements EnergySocketStrategy{

	@Override
	public Volt convertVolt(Volt volt) {
		return new Volt(volt.getVolts()/40);
	}

}
